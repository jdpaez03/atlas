[Unit]
Description=ATLAS virtual display (headed Chrome for the agents' browser)
After=network.target

[Service]
User=@USER@
ExecStart=/usr/bin/Xvfb :99 -screen 0 1920x1080x24 -nolisten tcp
Restart=always

[Install]
WantedBy=multi-user.target
